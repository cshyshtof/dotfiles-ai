---
paths:
  - **/*.md
  - **/*.mdc
---

# Markdown rules

## Chapters

- Don't create a table of contents
- Don't number chapters

## Tables

- Always format tables with fixed column widths so that they are readable even without a preview
- When formatting tables, calculate additional formatting characters (`*`, `_`, etc.) that affect the column width
